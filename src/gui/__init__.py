"""
GUI module - Now using Electron.

The GUI has been migrated to Electron for a modern cross-platform experience.
See the electron/ directory for the new implementation.

To run the GUI:
1. Start the API server: python -m uvicorn src.api.server:app --port 8765
2. Run the Electron app: cd electron && npm start
"""
